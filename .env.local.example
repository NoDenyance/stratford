# Vercel KV credentials — auto-populated when you connect a KV store in Vercel dashboard.
# For local dev: copy these values from your Vercel project's Storage > KV > .env.local tab.

KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
